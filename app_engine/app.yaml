runtime: python311

instance_class: F2

env_variables:
  BUCKET_NAME: "example-gcs-bucket"

handlers:
  # static_dir: static/images
  
- url: /.*
  secure: always
  #redirect_http_response_code: 301
  script: auto
  #thread_safe: true
